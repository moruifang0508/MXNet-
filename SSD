SSD：单发多框检测(single shot multibox detection)
组成：一个基础网络块和若干个多尺度特征块串联
基础网络块：用深度卷积神经网络来从原始图像中抽取特征，如用分类层之前截断的VGG、ResNet等
多尺度特征块：由上一层的特征图的高和宽缩小（如减半）
对每个特征图中所有像素点使用多个锚框分别进行类别预测和边界框预测
类别预测：如类别个数为q,每个像素点锚框个数为a，特征图高h，宽b，则总锚框数hba，类别预测输出的通道数为a(q+1)(类别0表示只含有背景）
边界框预测：预测每个锚框的左上角坐标和右下角坐标，通道数为4.
